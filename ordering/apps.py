"""App configuration for the 'ordering' app (registered in settings INSTALLED_APPS)."""
from django.apps import AppConfig


class OrderingConfig(AppConfig):
    name = 'ordering'      # the Python path/name of this app
